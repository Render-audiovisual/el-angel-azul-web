{
  "name": "el-angel-azul-web",
  "version": "0.1.0",
  "private": true,
  "description": "Web publica y panel admin de El Angel Azul.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "db:check": "node scripts/db-check.js",
    "check": "node --check server.js && node --check assets/js/app.js && npm test",
    "test": "node --test scripts/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1",
    "pg": "^8.22.0"
  }
}
